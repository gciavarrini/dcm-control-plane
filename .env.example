# Optional: copy to .env and set for local overrides or non-default credentials.
# Used by deploy/compose.yaml and test/subsystem/*/docker-compose.yaml.
# Defaults are for local dev and subsystem tests only.
# POSTGRES_USER=admin
# POSTGRES_PASSWORD=adminpass
#
# For the full platform stack (UI, service providers, image version pins),
# see deploy/.env.example.
